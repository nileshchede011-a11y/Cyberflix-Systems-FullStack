import { useEffect,useState } from "react"; import {Link} from "react-router-dom"; import Navbar from "../components/Navbar";
function Orders(){const [orders,setOrders]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{fetch('/api/orders').then(async r=>{const d=await r.json();if(!r.ok)throw Error(d.error||'Login required');setOrders(d.orders||[])}).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[]);return <><Navbar/><main className="orders-page"><section className="orders-header"><p className="eyebrow">CYBERFLIX SYSTEMS LLP</p><h1>MY ORDERS</h1><p>Track your orders from your Cyberflix account.</p></section>{loading?<p>Loading orders...</p>:error?<section className="empty-orders"><h2>{error}</h2><Link to="/login" className="continue-shopping-btn">LOGIN →</Link></section>:!orders.length?<section className="empty-orders"><h2>NO ORDERS YET</h2><Link to="/products" className="continue-shopping-btn">START SHOPPING →</Link></section>:<section className="orders-list">{orders.map(o=><article className="order-card" key={o.order_id}><div className="order-top"><div><span>ORDER ID</span><strong>{o.order_id}</strong></div><div><span>DATE</span><strong>{new Date(o.created_at).toLocaleString('en-IN')}</strong></div><div><span>STATUS</span><strong className="order-status">✓ {o.status}</strong></div></div><div className="order-products">{(o.items||[]).map(i=><div className="order-product" key={i.id}><img src={i.image} alt=""/><div><strong>{i.name}</strong><span>{i.brand} · Qty: {i.quantity}</span></div><b>₹{(i.price*i.quantity).toLocaleString('en-IN')}</b></div>)}</div><div className="order-bottom"><span>TOTAL</span><strong>₹{Number(o.total).toLocaleString('en-IN')}</strong></div></article>)}</section>}</main></>}; export default Orders;
